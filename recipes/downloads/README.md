# Downloads

Downloads accepts ordinary HTTP and HTTPS URLs from paired devices. It runs
one transfer at a time, keeps the rest queued, and preserves resumable partial
files in the selected destination.
